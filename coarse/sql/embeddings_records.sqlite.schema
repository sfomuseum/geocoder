CREATE TABLE {{ .Name  }} (
       id INTEGER PRIMARY KEY,
       record_id TEXT NOT NULL,
       model TEXT NOT NULL,
       language TEXT NOT NULL,
       tag TEXT NOT NULL
);

-- CREATE UNIQUE INDEX vec_record_model ON {{ .Name }} (record_id, model, language, tag);