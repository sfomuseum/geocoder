CREATE TABLE IF NOT EXISTS {{ .Name }} (
       rowid INTEGER PRIMARY KEY AUTOINCREMENT,
       minx DOUBLE,
       maxx DOUBLE,
       miny DOUBLE,
       maxy DOUBLE,
       record_id INTEGER NOT NULL
);

-- CREATE INDEX `bounds_by_recordid` ON {{ .Name }} (`record_id`);